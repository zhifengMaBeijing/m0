
本体对象模型位于物理存储和用户之间，动态本体对象建模是系统的核心。

图的建模：
尽量少的属性，其他字段可以从ES取得。

1.建模


统计分析？


类型

类型数据结构

类型之间的关系


2.配置管理

schema 可维护性

panlantires schema
   
   add data source to each object/record
   xml is used hold the rich data structure

metadata/system fields
   last_modified
   created_by
   time_created
Revisioning fields
    id： component id: propertiy id
	object_id: which object this property belongs to
	version: id + update time
	
	
	